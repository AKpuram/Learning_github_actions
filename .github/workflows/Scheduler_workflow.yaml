name: schedule jobs:

  on:
    schedule:
      - corn1: '*/5 * * * *'

jobs:
  run-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Python Script
        run: 
      
